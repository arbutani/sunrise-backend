import type { Dialect } from "sequelize";
export declare const config_dev: {
    database: {
        dialect: Dialect;
        host: string;
        port: number;
        username: string;
        password: string;
        database: string;
    };
};
